-To make things simple, just use the built in terminal in vscode

1. First make sure you are running the virtual environment.
    -in order to make it easy, pipenv is used for automatically keeping track of dependencies
        -MAKE SURE TO HAVE pipenv INSTALLED (JUST FOLLOW DOCUMENTATION ON THEIR WEBSITE)
2. Run the following: bash start.sh
    -This should run the server and the local host id should be in the terminal so just ctrl-click on it