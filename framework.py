from flask import Flask, render_template, url_for
app = Flask(__name__)

# only temp. replace with real templates once ready
#@app.route("/")
#@app.route("/home")
#def home():
#    return render_template("project-form.html")